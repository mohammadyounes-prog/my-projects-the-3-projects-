  <!DOCTYPE html>
  <html lang="en">
  <head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <title>Admin</title>
      <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  </head>
  <body>
      <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
          <div class="container-fluid">
              <a class="navbar-brand" href="#">Admin</a>
              <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                  <li class="nav-item">
                      <a class="nav-link" href="index.html">Generate Questions</a>
                  </li>
                  <li class="nav-item">
                      <a class="nav-link" href="retrieve_questions.html">Retrieve Questions</a>
                  </li>
                  <li class="nav-item">
                      <a class="nav-link" href="tasks.html">Task History</a>
                  </li>
                  <li class="nav-item">
                      <a class="nav-link" href="profile.html">Profile</a>
                  </li>
                  <li class="nav-item">
                      <a class="nav-link active" aria-current="page" href="admin.html">Admin</a>
                  </li>
              </ul>
              <button class="btn btn-outline-light" id="logoutButton">Logout</button>
          </div>
      </nav>

      <div class="container mt-4">
          <h1>Admin Management</h1>

          <div class="row mt-4">
              <div class="col-md-6">
                  <h2>User Management</h2>
                  <button class="btn btn-success mb-3" data-bs-toggle="modal" data-bs-target="#addUserModal">Add
  New User</button>
                  <table class="table table-striped">
                      <thead>
                          <tr>
                              <th>ID</th>
                              <th>Username</th>
                              <th>Is Admin</th>
                              <th>Actions</th>
                          </tr>
                      </thead>
                      <tbody id="userTableBody">
                          <!-- User rows will be loaded here -->
                      </tbody>
                  </table>

                  <!-- Add User Modal -->
                  <div class="modal fade" id="addUserModal" tabindex="-1" aria-labelledby="addUserModalLabel"
  aria-hidden="true">
                      <div class="modal-dialog">
                          <div class="modal-content">
                              <div class="modal-header">
                                  <h5 class="modal-title" id="addUserModalLabel">Add New User</h5>
                                  <button type="button" class="btn-close" data-bs-dismiss="modal"
  aria-label="Close"></button>
                              </div>
                              <div class="modal-body">
                                  <form id="addUserForm">
                                      <div class="mb-3">
                                          <label for="addUsername" class="form-label">Username</label>
                                          <input type="text" class="form-control" id="addUsername" required>
                                      </div>
                                      <div class="mb-3">
                                          <label for="addPassword" class="form-label">Password</label>
                                          <input type="password" class="form-control" id="addPassword" required>
                                      </div>
                                      <div class="mb-3 form-check">
                                          <input type="checkbox" class="form-check-input" id="addIsAdmin">
                                          <label class="form-check-label" for="addIsAdmin">Is Admin</label>
                                      </div>
                                      <button type="submit" class="btn btn-primary">Add User</button>
                                  </form>
                              </div>
                          </div>
                      </div>
                  </div>

                  <!-- Edit User Modal -->
                  <div class="modal fade" id="editUserModal" tabindex="-1" aria-labelledby="editUserModalLabel"
  aria-hidden="true">
                      <div class="modal-dialog">
                          <div class="modal-content">
                              <div class="modal-header">
                                  <h5 class="modal-title" id="editUserModalLabel">Edit User</h5>
                                  <button type="button" class="btn-close" data-bs-dismiss="modal"
  aria-label="Close"></button>
                              </div>
                              <div class="modal-body">
                                  <form id="editUserForm">
                                      <input type="hidden" id="editUserId">
                                      <div class="mb-3">
                                          <label for="editUsername" class="form-label">Username</label>
                                          <input type="text" class="form-control" id="editUsername" required>
                                      </div>
                                      <div class="mb-3">
                                          <label for="editPassword" class="form-label">New Password (leave blank
  to keep current)</label>
                                          <input type="password" class="form-control" id="editPassword">
                                      </div>
                                      <div class="mb-3 form-check">
                                          <input type="checkbox" class="form-check-input" id="editIsAdmin">
                                          <label class="form-check-label" for="editIsAdmin">Is Admin</label>
                                      </div>
                                      <button type="submit" class="btn btn-primary">Save Changes</button>
                                  </form>
                              </div>
                          </div>
                      </div>
                  </div>
              </div>
              <div class="col-md-6">
                  <h2>Question Properties</h2>
                  <ul class="nav nav-tabs" id="propertyTabs" role="tablist">
                      <li class="nav-item" role="presentation">
                          <button class="nav-link active" id="difficulty-tab" data-bs-toggle="tab"
  data-bs-target="#difficulty" type="button" role="tab" aria-controls="difficulty"
  aria-selected="true">Difficulty Levels</button>
                      </li>
                      <li class="nav-item" role="presentation">
                          <button class="nav-link" id="cognitive-tab" data-bs-toggle="tab"
  data-bs-target="#cognitive" type="button" role="tab" aria-controls="cognitive" aria-selected="false">Cognitive
  Levels</button>
                      </li>
                      <li class="nav-item" role="presentation">
                          <button class="nav-link" id="learning-tab" data-bs-toggle="tab"
  data-bs-target="#learning" type="button" role="tab" aria-controls="learning" aria-selected="false">Learning
  Outcomes</button>
                      </li>
                  </ul>
                  <div class="tab-content" id="propertyTabsContent">
                      <!-- Difficulty Levels Tab -->
                      <div class="tab-pane fade show active" id="difficulty" role="tabpanel"
  aria-labelledby="difficulty-tab">
                          <h3 class="mt-3">Difficulty Levels</h3>
                          <button class="btn btn-success mb-3" data-bs-toggle="modal"
  data-bs-target="#addPropertyModal" data-property-type="difficulty_levels">Add New Difficulty</button>
                          <table class="table table-striped">
                              <thead>
                                  <tr>
                                      <th>ID</th>
                                      <th>Name</th>
                                      <th>Actions</th>
                                  </tr>
                              </thead>
                              <tbody id="difficultyTableBody">
                                  <!-- Difficulty levels will be loaded here -->
                              </tbody>
                          </table>
                      </div>
                      <!-- Cognitive Levels Tab -->
                      <div class="tab-pane fade" id="cognitive" role="tabpanel" aria-labelledby="cognitive-tab">
                          <h3 class="mt-3">Cognitive Levels</h3>
                          <button class="btn btn-success mb-3" data-bs-toggle="modal"
  data-bs-target="#addPropertyModal" data-property-type="cognitive_levels">Add New Cognitive Level</button>
                          <table class="table table-striped">
                              <thead>
                                  <tr>
                                      <th>ID</th>
                                      <th>Name</th>
                                      <th>Actions</th>
                                  </tr>
                              </thead>
                              <tbody id="cognitiveTableBody">
                                  <!-- Cognitive levels will be loaded here -->
                              </tbody>
                          </table>
                      </div>
                      <!-- Learning Outcomes Tab -->
                      <div class="tab-pane fade" id="learning" role="tabpanel" aria-labelledby="learning-tab">
                          <h3 class="mt-3">Learning Outcomes</h3>
                          <button class="btn btn-success mb-3" data-bs-toggle="modal"
  data-bs-target="#addPropertyModal" data-property-type="learning_outcomes">Add New Learning Outcome</button>
                          <table class="table table-striped">
                              <thead>
                                  <tr>
                                      <th>ID</th>
                                      <th>Name</th>
                                      <th>Actions</th>
                                  </tr>
                              </thead>
                              <tbody id="learningTableBody">
                                  <!-- Learning outcomes will be loaded here -->
                              </tbody>
                          </table>
                      </div>
                  </div>

                  <!-- Add Property Modal -->
                  <div class="modal fade" id="addPropertyModal" tabindex="-1"
  aria-labelledby="addPropertyModalLabel" aria-hidden="true">
                      <div class="modal-dialog">
                          <div class="modal-content">
                              <div class="modal-header">
                                  <h5 class="modal-title" id="addPropertyModalLabel">Add New Property</h5>
                                  <button type="button" class="btn-close" data-bs-dismiss="modal"
  aria-label="Close"></button>
                              </div>
                              <div class="modal-body">
                                  <form id="addPropertyForm">
                                      <input type="hidden" id="addPropertyType">
                                      <div class="mb-3">
                                          <label for="addPropertyName" class="form-label">Property Name</label>
                                          <input type="text" class="form-control" id="addPropertyName" required>
                                      </div>
                                      <button type="submit" class="btn btn-primary">Add Property</button>
                                  </form>
                              </div>
                          </div>
                      </div>
                  </div>

                  <!-- Edit Property Modal -->
                  <div class="modal fade" id="editPropertyModal" tabindex="-1"
  aria-labelledby="editPropertyModalLabel" aria-hidden="true">
                      <div class="modal-dialog">
                          <div class="modal-content">
                              <div class="modal-header">
                                  <h5 class="modal-title" id="editPropertyModalLabel">Edit Property</h5>
                                  <button type="button" class="btn-close" data-bs-dismiss="modal"
  aria-label="Close"></button>
                              </div>
                              <div class="modal-body">
                                  <form id="editPropertyForm">
                                      <input type="hidden" id="editPropertyType">
                                      <input type="hidden" id="editPropertyId">
                                      <div class="mb-3">
                                          <label for="editPropertyName" class="form-label">Property Name</label>
                                          <input type="text" class="form-control" id="editPropertyName" required>
                                      </div>
                                      <button type="submit" class="btn btn-primary">Save Changes</button>
                                  </form>
                              </div>
                          </div>
                      </div>
                  </div>
              </div>
          </div>
      </div>

      <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
      <script>
          const API_BASE_URL = "http://127.0.0.1:8000"; // Adjust if your backend runs on a different port

          // Helper to get auth headers
          function getAuthHeaders() {
              const token = localStorage.getItem('accessToken');
              return {
                  'Authorization': Bearer ${token},
                  'Content-Type': 'application/json'
              };
          }

          // --- User Management Functions ---
          async function loadUsers() {
              try {
                  const response = await fetch(${API_BASE_URL}/admin/users, {
                      headers: getAuthHeaders()
                  });
                  if (!response.ok) {
                      throw new Error(HTTP error! status: ${response.status});
                  }
                  const users = await response.json();
                  console.log("Loaded users:", users); // Debugging line
                  const userTableBody = document.getElementById('userTableBody');
                  userTableBody.innerHTML = '';
                  users.forEach(user => {
                      const row = userTableBody.insertRow();
                      row.innerHTML = `
                          <td>${user.id}</td>
                          <td>${user.username}</td>
                          <td>${user.is_admin ? 'Yes' : 'No'}</td>
                          <td>
                              <button class="btn btn-sm btn-primary edit-user-btn" data-id="${user.id}"
  data-username="${user.username}" data-isadmin="${user.is_admin}">Edit</button>
                              <button class="btn btn-sm btn-danger delete-user-btn"
  data-id="${user.id}">Delete</button>
                          </td>
                      `;
                  });
                  attachUserEventListeners();
              } catch (error) {
                  console.error('Error loading users:', error);
                  alert('Failed to load users.');
              }
          }

          function attachUserEventListeners() {
              document.querySelectorAll('.edit-user-btn').forEach(button => {
                  button.onclick = (event) => {
                      const id = event.target.dataset.id;
                      const username = event.target.dataset.username;
                      const isAdmin = event.target.dataset.isadmin === 'true';

                      document.getElementById('editUserId').value = id;
                      document.getElementById('editUsername').value = username;
                      document.getElementById('editIsAdmin').checked = isAdmin;

                      const editUserModal = new bootstrap.Modal(document.getElementById('editUserModal'));
                      editUserModal.show();
                  };
              });

              document.querySelectorAll('.delete-user-btn').forEach(button => {
                  button.onclick = async (event) => {
                      const id = event.target.dataset.id;
                      if (confirm(Are you sure you want to delete user with ID ${id}?)) {
                          try {
                              const response = await fetch(${API_BASE_URL}/admin/users/${id}, {
                                  method: 'DELETE',
                                  headers: getAuthHeaders()
                              });
                              if (!response.ok) {
                                  throw new Error(HTTP error! status: ${response.status});
                              }
                              alert('User deleted successfully!');
                              loadUsers();
                          } catch (error) {
                              console.error('Error deleting user:', error);
                              alert('Failed to delete user.');
                          }
                      }
                  };
              });
          }

          document.getElementById('addUserForm').addEventListener('submit', async function(event) {
              event.preventDefault();
              const username = document.getElementById('addUsername').value;
              const password = document.getElementById('addPassword').value;
              const isAdmin = document.getElementById('addIsAdmin').checked;

              try {
                  const response = await fetch(${API_BASE_URL}/admin/users, {
                      method: 'POST',
                      headers: getAuthHeaders(),
                      body: JSON.stringify({ username, password, is_admin: isAdmin })
                  });
                  if (!response.ok) {
                      const errorData = await response.json();
                      throw new Error(errorData.detail || HTTP error! status: ${response.status});
                  }
                  alert('User added successfully!');
                  bootstrap.Modal.getInstance(document.getElementById('addUserModal')).hide();
                  this.reset();
                  loadUsers();
              } catch (error) {
                  console.error('Error adding user:', error);
                  alert(Failed to add user: ${error.message});
              }
          });

          document.getElementById('editUserForm').addEventListener('submit', async function(event) {
              event.preventDefault();
              const id = document.getElementById('editUserId').value;
              const username = document.getElementById('editUsername').value;
              const password = document.getElementById('editPassword').value;
              const isAdmin = document.getElementById('editIsAdmin').checked;

              const updateData = { username, is_admin: isAdmin };
              if (password) {
                  updateData.password = password;
              }

              try {
                  const response = await fetch(${API_BASE_URL}/admin/users/${id}, {
                      method: 'PUT',
                      headers: getAuthHeaders(),
                      body: JSON.stringify(updateData)
                  });
                  if (!response.ok) {
                      const errorData = await response.json();
                      throw new Error(errorData.detail || HTTP error! status: ${response.status});
                  }
                  alert('User updated successfully!');
                  bootstrap.Modal.getInstance(document.getElementById('editUserModal')).hide();
                  loadUsers();
              } catch (error) {
                  console.error('Error updating user:', error);
                  alert(Failed to update user: ${error.message});
              }
          });

          // --- Question Property Management Functions ---
          const propertyTypeMap = {
              'difficulty': 'difficulty_levels',
              'cognitive': 'cognitive_levels',
              'learning': 'learning_outcomes'
          };

          async function loadProperties(propertyType) {
              try {
                  const response = await fetch(${API_BASE_URL}/admin/properties/${propertyType}, {
                      headers: getAuthHeaders()
                  });
                  if (!response.ok) {
                      throw new Error(HTTP error! status: ${response.status});
                  }
                  const properties = await response.json();
                  console.log("Loaded properties for", propertyType, ":", properties); // Debugging line
                  const tableBodyId = ${Object.keys(propertyTypeMap).find(key => propertyTypeMap[key] ===
  propertyType)}TableBody;
                  const tableBody = document.getElementById(tableBodyId);
                  tableBody.innerHTML = '';
                  properties.forEach(prop => {
                      const row = tableBody.insertRow();
                      row.innerHTML = `
                          <td>${prop.id || 'N/A'}</td>
                          <td>${prop.name}</td>
                          <td>
                              <button class="btn btn-sm btn-primary edit-property-btn" data-id="${prop.id}"
  data-name="${prop.name}" data-property-type="${propertyType}">Edit</button>
                              <button class="btn btn-sm btn-danger delete-property-btn" data-id="${prop.id}"
  data-property-type="${propertyType}">Delete</button>
                          </td>
                      `;
                  });
                  attachPropertyEventListeners();
              } catch (error) {
                  console.error(Error loading ${propertyType}:, error);
                  alert(Failed to load ${propertyType}.);
              }
          }

          function attachPropertyEventListeners() {
              document.querySelectorAll('.edit-property-btn').forEach(button => {
                  button.onclick = (event) => {
                      const id = event.target.dataset.id;
                      const name = event.target.dataset.name;
                      const propertyType = event.target.dataset.propertyType;

                      document.getElementById('editPropertyId').value = id;
                      document.getElementById('editPropertyName').value = name;
                      document.getElementById('editPropertyType').value = propertyType;

                      const editPropertyModal = new
  bootstrap.Modal(document.getElementById('editPropertyModal'));
                      editPropertyModal.show();
                  };
              });

              document.querySelectorAll('.delete-property-btn').forEach(button => {
                  button.onclick = async (event) => {
                      const id = event.target.dataset.id;
                      const propertyType = event.target.dataset.propertyType;
                      if (confirm(Are you sure you want to delete this ${propertyType} item with ID ${id}?)) {
                          try {
                              const response = await
  fetch(${API_BASE_URL}/admin/properties/${propertyType}/${id}, {
                                  method: 'DELETE',
                                  headers: getAuthHeaders()
                              });
                              if (!response.ok) {
                                  throw new Error(HTTP error! status: ${response.status});
                              }
                              alert('Property deleted successfully!');
                              loadProperties(propertyType);
                          } catch (error) {
                              console.error('Error deleting property:', error);
                              alert('Failed to delete property.');
                          }
                      }
                  };
              });
          }

          document.getElementById('addPropertyForm').addEventListener('submit', async function(event) {
              event.preventDefault();
              const propertyType = document.getElementById('addPropertyType').value;
              const name = document.getElementById('addPropertyName').value;

              try {
                  const response = await fetch(${API_BASE_URL}/admin/properties/${propertyType}, {
                      method: 'POST',
                      headers: getAuthHeaders(),
                      body: JSON.stringify({ name })
                  });
                  if (!response.ok) {
                      const errorData = await response.json();
                      throw new Error(errorData.detail || HTTP error! status: ${response.status});
                  }
                  alert('Property added successfully!');
                  bootstrap.Modal.getInstance(document.getElementById('addPropertyModal')).hide();
                  this.reset();
                  loadProperties(propertyType);
              } catch (error) {
                  console.error('Error adding property:', error);
                  alert(Failed to add property: ${error.message});
              }
          });

          document.getElementById('editPropertyForm').addEventListener('submit', async function(event) {
              event.preventDefault();
              const propertyType = document.getElementById('editPropertyType').value;
              const id = document.getElementById('editPropertyId').value;
              const name = document.getElementById('editPropertyName').value;

              try {
                  const response = await fetch(${API_BASE_URL}/admin/properties/${propertyType}/${id}, {
                      method: 'PUT',
                      headers: getAuthHeaders(),
                      body: JSON.stringify({ name })
                  });
                  if (!response.ok) {
                      const errorData = await response.json();
                      throw new Error(errorData.detail || HTTP error! status: ${response.status});
                  }
                  alert('Property updated successfully!');
                  bootstrap.Modal.getInstance(document.getElementById('editPropertyModal')).hide();
                  loadProperties(propertyType);
              } catch (error) {
                  console.error('Error updating property:', error);
                  alert(Failed to update property: ${error.message});
              }
          });

          // Event listener for property tabs to load data when activated
          document.getElementById('propertyTabs').addEventListener('shown.bs.tab', function (event) {
              const activeTabId = event.target.id; // e.g., 'difficulty-tab'
              let propertyType = '';
              if (activeTabId === 'difficulty-tab') {
                  propertyType = propertyTypeMap.difficulty;
              } else if (activeTabId === 'cognitive-tab') {
                  propertyType = propertyTypeMap.cognitive;
              } else if (activeTabId === 'learning-tab') {
                  propertyType = propertyTypeMap.learning;
              }
              if (propertyType) {
                  loadProperties(propertyType);
              }
          });

          // Event listener for Add Property buttons to set the property type in the modal
          document.querySelectorAll('[data-bs-target="#addPropertyModal"]').forEach(button => {
              button.addEventListener('click', function() {
                  const propertyType = this.dataset.propertyType;
                  document.getElementById('addPropertyType').value = propertyType;
                  document.getElementById('addPropertyModalLabel').textContent = Add New
  ${propertyType.replace('_', ' ').replace(/levels$/, '').trim()} Property;
              });
          });

          // Initial load
          document.addEventListener('DOMContentLoaded', () => {
              document.getElementById('logoutButton').addEventListener('click', function() {
                  localStorage.removeItem('accessToken');
                  localStorage.removeItem('isAdmin');
                  window.location.href = 'login.html';
              });
              loadUsers();
              // Load default active tab's properties
              loadProperties(propertyTypeMap.difficulty);
          });
      </script>
  </body>
  </html>
  `


╭───────────────────────────────────────────────────────────────────────────────────────────╮
│  > admin.html:258 Uncaught SyntaxError: Unexpected identifier '$' (at admin.html:258:43)  │
╰───────────────────────────────────────────────────────────────────────────────────────────╯

✦ This is very persistent. The SyntaxError: Unexpected identifier '$' means that the JavaScript engine is not
  recognizing the template literal (the string enclosed in backticks `  `) and is instead treating the $` as a
  regular character, which is invalid syntax outside of a template literal.


  This almost always happens when the backticks (`  `) are accidentally replaced by single quotes (') or double
  quotes ("`) during copying or saving the file.

  Please do the following very carefully:


   1. Open your `D:\QuestionRetrieval\new-q-bank\frontend\admin.html` file in a plain text editor (like Notepad on
      Windows, VS Code, Sublime Text, etc. – not a web browser).
  `javascript
      row.innerHTML = `
          <td>${user.id}</td>
          <td>${user.username}</td>
          <td>${user.is_admin ? 'Yes' : 'No'}</td>
          <td>
              <button class="btn btn-sm btn-primary edit-user-btn" data-id="${user.id}"
  data-username="${user.username}" data-isadmin="${user.is_admin}">Edit</button>
              <button class="btn btn-sm btn-danger delete-user-btn" data-id="${user.id}">Delete</button>
          </td>
      `;
      `
      Pay special attention to the characters immediately after `row.innerHTML = ` and before the final
  semicolon `;`. They *must* be backticks.

  5.  Do the same verification for the `row.innerHTML = ` line inside the `loadProperties()` function.


  If you find any single or double quotes instead of backticks, please change them to backticks, save the file,
  restart the server, and hard refresh your browser.


  If the backticks are definitely there, please let me know, and we'll explore other possibilities.

╭────────────────────────────────────────────────────────────────╮
│  > javascript is not included in the file , should i add it ?  │
╰────────────────────────────────────────────────────────────────╯

✦ Yes, the JavaScript must be included in the file within <script> tags. The errors you're seeing are precisely
  because the browser is trying to interpret parts of the file as JavaScript when it's not properly defined as
  such.


  It seems there might have been an issue with the previous copy-paste, or the <script> tags themselves were
  removed.

  Please replace the ENTIRE content of your `D:\QuestionRetrieval\new-q-bank\frontend\admin.html` file with the
  complete, correct HTML and JavaScript provided below.


  This version includes all the HTML structure and the JavaScript logic, properly enclosed within <script> tags
  at the end of the <body>.


  `html
  <!DOCTYPE html>
  <html lang="en">
  <head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <title>Admin</title>
      <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  </head>
  <body>
      <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
          <div class="container-fluid">
              <a class="navbar-brand" href="#">Admin</a>
              <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                  <li class="nav-item">
                      <a class="nav-link" href="index.html">Generate Questions</a>
                  </li>
                  <li class="nav-item">
                      <a class="nav-link" href="retrieve_questions.html">Retrieve Questions</a>
                  </li>
                  <li class="nav-item">
                      <a class="nav-link" href="tasks.html">Task History</a>
                  </li>
                  <li class="nav-item">
                      <a class="nav-link" href="profile.html">Profile</a>
                  </li>
                  <li class="nav-item">
                      <a class="nav-link active" aria-current="page" href="admin.html">Admin</a>
                  </li>
              </ul>
              <button class="btn btn-outline-light" id="logoutButton">Logout</button>
          </div>
      </nav>

      <div class="container mt-4">
          <h1>Admin Management</h1>

          <div class="row mt-4">
              <div class="col-md-6">
                  <h2>User Management</h2>
                  <button class="btn btn-success mb-3" data-bs-toggle="modal" data-bs-target="#addUserModal">Add
  New User</button>
                  <table class="table table-striped">
                      <thead>
                          <tr>
                              <th>ID</th>
                              <th>Username</th>
                              <th>Is Admin</th>
                              <th>Actions</th>
                          </tr>
                      </thead>
                      <tbody id="userTableBody">
                          <!-- User rows will be loaded here -->
                      </tbody>
                  </table>

                  <!-- Add User Modal -->
                  <div class="modal fade" id="addUserModal" tabindex="-1" aria-labelledby="addUserModalLabel"
  aria-hidden="true">
                      <div class="modal-dialog">
                          <div class="modal-content">
                              <div class="modal-header">
                                  <h5 class="modal-title" id="addUserModalLabel">Add New User</h5>
                                  <button type="button" class="btn-close" data-bs-dismiss="modal"
  aria-label="Close"></button>
                              </div>
                              <div class="modal-body">
                                  <form id="addUserForm">
                                      <div class="mb-3">
                                          <label for="addUsername" class="form-label">Username</label>
                                          <input type="text" class="form-control" id="addUsername" required>
                                      </div>
                                      <div class="mb-3">
                                          <label for="addPassword" class="form-label">Password</label>
                                          <input type="password" class="form-control" id="addPassword" required>
                                      </div>
                                      <div class="mb-3 form-check">
                                          <input type="checkbox" class="form-check-input" id="addIsAdmin">
                                          <label class="form-check-label" for="addIsAdmin">Is Admin</label>
                                      </div>
                                      <button type="submit" class="btn btn-primary">Add User</button>
                                  </form>
                              </div>
                          </div>
                      </div>
                  </div>

                  <!-- Edit User Modal -->
                  <div class="modal fade" id="editUserModal" tabindex="-1" aria-labelledby="editUserModalLabel"
  aria-hidden="true">
                      <div class="modal-dialog">
                          <div class="modal-content">
                              <div class="modal-header">
                                  <h5 class="modal-title" id="editUserModalLabel">Edit User</h5>
                                  <button type="button" class="btn-close" data-bs-dismiss="modal"
  aria-label="Close"></button>
                              </div>
                              <div class="modal-body">
                                  <form id="editUserForm">
                                      <input type="hidden" id="editUserId">
                                      <div class="mb-3">
                                          <label for="editUsername" class="form-label">Username</label>
                                          <input type="text" class="form-control" id="editUsername" required>
                                      </div>
                                      <div class="mb-3">
                                          <label for="editPassword" class="form-label">New Password (leave blank
  to keep current)</label>
                                          <input type="password" class="form-control" id="editPassword">
                                      </div>
                                      <div class="mb-3 form-check">
                                          <input type="checkbox" class="form-check-input" id="editIsAdmin">
                                          <label class="form-check-label" for="editIsAdmin">Is Admin</label>
                                      </div>
                                      <button type="submit" class="btn btn-primary">Save Changes</button>
                                  </form>
                              </div>
                          </div>
                      </div>
                  </div>
              </div>
              <div class="col-md-6">
                  <h2>Question Properties</h2>
                  <ul class="nav nav-tabs" id="propertyTabs" role="tablist">
                      <li class="nav-item" role="presentation">
                          <button class="nav-link active" id="difficulty-tab" data-bs-toggle="tab"
  data-bs-target="#difficulty" type="button" role="tab" aria-controls="difficulty"
  aria-selected="true">Difficulty Levels</button>
                      </li>
                      <li class="nav-item" role="presentation">
                          <button class="nav-link" id="cognitive-tab" data-bs-toggle="tab"
  data-bs-target="#cognitive" type="button" role="tab" aria-controls="cognitive" aria-selected="false">Cognitive
  Levels</button>
                      </li>
                      <li class="nav-item" role="presentation">
                          <button class="nav-link" id="learning-tab" data-bs-toggle="tab"
  data-bs-target="#learning" type="button" role="tab" aria-controls="learning" aria-selected="false">Learning
  Outcomes</button>
                      </li>
                  </ul>
                  <div class="tab-content" id="propertyTabsContent">
                      <!-- Difficulty Levels Tab -->
                      <div class="tab-pane fade show active" id="difficulty" role="tabpanel"
  aria-labelledby="difficulty-tab">
                          <h3 class="mt-3">Difficulty Levels</h3>
                          <button class="btn btn-success mb-3" data-bs-toggle="modal"
  data-bs-target="#addPropertyModal" data-property-type="difficulty_levels">Add New Difficulty</button>
                          <table class="table table-striped">
                              <thead>
                                  <tr>
                                      <th>ID</th>
                                      <th>Name</th>
                                      <th>Actions</th>
                                  </tr>
                              </thead>
                              <tbody id="difficultyTableBody">
                                  <!-- Difficulty levels will be loaded here -->
                              </tbody>
                          </table>
                      </div>
                      <!-- Cognitive Levels Tab -->
                      <div class="tab-pane fade" id="cognitive" role="tabpanel" aria-labelledby="cognitive-tab">
                          <h3 class="mt-3">Cognitive Levels</h3>
                          <button class="btn btn-success mb-3" data-bs-toggle="modal"
  data-bs-target="#addPropertyModal" data-property-type="cognitive_levels">Add New Cognitive Level</button>
                          <table class="table table-striped">
                              <thead>
                                  <tr>
                                      <th>ID</th>
                                      <th>Name</th>
                                      <th>Actions</th>
                                  </tr>
                              </thead>
                              <tbody id="cognitiveTableBody">
                                  <!-- Cognitive levels will be loaded here -->
                              </tbody>
                          </table>
                      </div>
                      <!-- Learning Outcomes Tab -->
                      <div class="tab-pane fade" id="learning" role="tabpanel" aria-labelledby="learning-tab">
                          <h3 class="mt-3">Learning Outcomes</h3>
                          <button class="btn btn-success mb-3" data-bs-toggle="modal"
  data-bs-target="#addPropertyModal" data-property-type="learning_outcomes">Add New Learning Outcome</button>
                          <table class="table table-striped">
                              <thead>
                                  <tr>
                                      <th>ID</th>
                                      <th>Name</th>
                                      <th>Actions</th>
                                  </tr>
                              </thead>
                              <tbody id="learningTableBody">
                                  <!-- Learning outcomes will be loaded here -->
                              </tbody>
                          </table>
                      </div>
                  </div>

                  <!-- Add Property Modal -->
                  <div class="modal fade" id="addPropertyModal" tabindex="-1"
  aria-labelledby="addPropertyModalLabel" aria-hidden="true">
                      <div class="modal-dialog">
                          <div class="modal-content">
                              <div class="modal-header">
                                  <h5 class="modal-title" id="addPropertyModalLabel">Add New Property</h5>
                                  <button type="button" class="btn-close" data-bs-dismiss="modal"
  aria-label="Close"></button>
                              </div>
                              <div class="modal-body">
                                  <form id="addPropertyForm">
                                      <input type="hidden" id="addPropertyType">
                                      <div class="mb-3">
                                          <label for="addPropertyName" class="form-label">Property Name</label>
                                          <input type="text" class="form-control" id="addPropertyName" required>
                                      </div>
                                      <button type="submit" class="btn btn-primary">Add Property</button>
                                  </form>
                              </div>
                          </div>
                      </div>
                  </div>

                  <!-- Edit Property Modal -->
                  <div class="modal fade" id="editPropertyModal" tabindex="-1"
  aria-labelledby="editPropertyModalLabel" aria-hidden="true">
                      <div class="modal-dialog">
                          <div class="modal-content">
                              <div class="modal-header">
                                  <h5 class="modal-title" id="editPropertyModalLabel">Edit Property</h5>
                                  <button type="button" class="btn-close" data-bs-dismiss="modal"
  aria-label="Close"></button>
                              </div>
                              <div class="modal-body">
                                  <form id="editPropertyForm">
                                      <input type="hidden" id="editPropertyType">
                                      <input type="hidden" id="editPropertyId">
                                      <div class="mb-3">
                                          <label for="editPropertyName" class="form-label">Property Name</label>
                                          <input type="text" class="form-control" id="editPropertyName" required>
                                      </div>
                                      <button type="submit" class="btn btn-primary">Save Changes</button>
                                  </form>
                              </div>
                          </div>
                      </div>
                  </div>
              </div>
          </div>
      </div>

      <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
      <script>
          const API_BASE_URL = "http://127.0.0.1:8000"; // Adjust if your backend runs on a different port

          // Helper to get auth headers
          function getAuthHeaders() {
              const token = localStorage.getItem('accessToken');
              return {
                  'Authorization': Bearer ${token},
                  'Content-Type': 'application/json'
              };
          }

          // --- User Management Functions ---
          async function loadUsers() {
              try {
                  const response = await fetch(${API_BASE_URL}/admin/users, {
                      headers: getAuthHeaders()
                  });
                  if (!response.ok) {
                      throw new Error(HTTP error! status: ${response.status});
                  }
                  const users = await response.json();
                  console.log("Loaded users:", users); // Debugging line
                  const userTableBody = document.getElementById('userTableBody');
                  userTableBody.innerHTML = '';
                  users.forEach(user => {
                      const row = userTableBody.insertRow();
                      row.innerHTML = `
                          <td>${user.id}</td>
                          <td>${user.username}</td>
                          <td>${user.is_admin ? 'Yes' : 'No'}</td>
                          <td>
                              <button class="btn btn-sm btn-primary edit-user-btn" data-id="${user.id}"
  data-username="${user.username}" data-isadmin="${user.is_admin}">Edit</button>
                              <button class="btn btn-sm btn-danger delete-user-btn"
  data-id="${user.id}">Delete</button>
                          </td>
                      `;
                  });
                  attachUserEventListeners();
              } catch (error) {
                  console.error('Error loading users:', error);
                  alert('Failed to load users.');
              }
          }

          function attachUserEventListeners() {
              document.querySelectorAll('.edit-user-btn').forEach(button => {
                  button.onclick = (event) => {
                      const id = event.target.dataset.id;
                      const username = event.target.dataset.username;
                      const isAdmin = event.target.dataset.isadmin === 'true';

                      document.getElementById('editUserId').value = id;
                      document.getElementById('editUsername').value = username;
                      document.getElementById('editIsAdmin').checked = isAdmin;

                      const editUserModal = new bootstrap.Modal(document.getElementById('editUserModal'));
                      editUserModal.show();
                  };
              });

              document.querySelectorAll('.delete-user-btn').forEach(button => {
                  button.onclick = async (event) => {
                      const id = event.target.dataset.id;
                      if (confirm(Are you sure you want to delete user with ID ${id}?)) {
                          try {
                              const response = await fetch(${API_BASE_URL}/admin/users/${id}, {
                                  method: 'DELETE',
                                  headers: getAuthHeaders()
                              });
                              if (!response.ok) {
                                  throw new Error(HTTP error! status: ${response.status});
                              }
                              alert('User deleted successfully!');
                              loadUsers();
                          } catch (error) {
                              console.error('Error deleting user:', error);
                              alert('Failed to delete user.');
                          }
                      }
                  };
              });
          }

          document.getElementById('addUserForm').addEventListener('submit', async function(event) {
              event.preventDefault();
              const username = document.getElementById('addUsername').value;
              const password = document.getElementById('addPassword').value;
              const isAdmin = document.getElementById('addIsAdmin').checked;

              try {
                  const response = await fetch(${API_BASE_URL}/admin/users, {
                      method: 'POST',
                      headers: getAuthHeaders(),
                      body: JSON.stringify({ username, password, is_admin: isAdmin })
                  });
                  if (!response.ok) {
                      const errorData = await response.json();
                      throw new Error(errorData.detail || HTTP error! status: ${response.status});
                  }
                  alert('User added successfully!');
                  bootstrap.Modal.getInstance(document.getElementById('addUserModal')).hide();
                  this.reset();
                  loadUsers();
              } catch (error) {
                  console.error('Error adding user:', error);
                  alert(Failed to add user: ${error.message});
              }
          });

          document.getElementById('editUserForm').addEventListener('submit', async function(event) {
              event.preventDefault();
              const id = document.getElementById('editUserId').value;
              const username = document.getElementById('editUsername').value;
              const password = document.getElementById('editPassword').value;
              const isAdmin = document.getElementById('editIsAdmin').checked;

              const updateData = { username, is_admin: isAdmin };
              if (password) {
                  updateData.password = password;
              }

              try {
                  const response = await fetch(${API_BASE_URL}/admin/users/${id}, {
                      method: 'PUT',
                      headers: getAuthHeaders(),
                      body: JSON.stringify(updateData)
                  });
                  if (!response.ok) {
                      const errorData = await response.json();
                      throw new Error(errorData.detail || HTTP error! status: ${response.status});
                  }
                  alert('User updated successfully!');
                  bootstrap.Modal.getInstance(document.getElementById('editUserModal')).hide();
                  loadUsers();
              } catch (error) {
                  console.error('Error updating user:', error);
                  alert(Failed to update user: ${error.message});
              }
          });

          // --- Question Property Management Functions ---
          const propertyTypeMap = {
              'difficulty': 'difficulty_levels',
              'cognitive': 'cognitive_levels',
              'learning': 'learning_outcomes'
          };

          async function loadProperties(propertyType) {
              try {
                  const response = await fetch(${API_BASE_URL}/admin/properties/${propertyType}, {
                      headers: getAuthHeaders()
                  });
                  if (!response.ok) {
                      throw new Error(HTTP error! status: ${response.status});
                  }
                  const properties = await response.json();
                  console.log("Loaded properties for", propertyType, ":", properties); // Debugging line
                  const tableBodyId = ${Object.keys(propertyTypeMap).find(key => propertyTypeMap[key] ===
  propertyType)}TableBody;
                  const tableBody = document.getElementById(tableBodyId);
                  tableBody.innerHTML = '';
                  properties.forEach(prop => {
                      const row = tableBody.insertRow();
                      row.innerHTML = `
                          <td>${prop.id || 'N/A'}</td>
                          <td>${prop.name}</td>
                          <td>
                              <button class="btn btn-sm btn-primary edit-property-btn" data-id="${prop.id}"
  data-name="${prop.name}" data-property-type="${propertyType}">Edit</button>
                              <button class="btn btn-sm btn-danger delete-property-btn" data-id="${prop.id}"
  data-property-type="${propertyType}">Delete</button>
                          </td>
                      `;
                  });
                  attachPropertyEventListeners();
              } catch (error) {
                  console.error(Error loading ${propertyType}:, error);
                  alert(Failed to load ${propertyType}.);
              }
          }

          function attachPropertyEventListeners() {
              document.querySelectorAll('.edit-property-btn').forEach(button => {
                  button.onclick = (event) => {
                      const id = event.target.dataset.id;
                      const name = event.target.dataset.name;
                      const propertyType = event.target.dataset.propertyType;

                      document.getElementById('editPropertyId').value = id;
                      document.getElementById('editPropertyName').value = name;
                      document.getElementById('editPropertyType').value = propertyType;

                      const editPropertyModal = new
  bootstrap.Modal(document.getElementById('editPropertyModal'));
                      editPropertyModal.show();
                  };
              });

              document.querySelectorAll('.delete-property-btn').forEach(button => {
                  button.onclick = async (event) => {
                      const id = event.target.dataset.id;
                      const propertyType = event.target.dataset.propertyType;
                      if (confirm(Are you sure you want to delete this ${propertyType} item with ID ${id}?)) {
                          try {
                              const response = await
  fetch(${API_BASE_URL}/admin/properties/${propertyType}/${id}, {
                                  method: 'DELETE',
                                  headers: getAuthHeaders()
                              });
                              if (!response.ok) {
                                  throw new Error(HTTP error! status: ${response.status});
                              }
                              alert('Property deleted successfully!');
                              loadProperties(propertyType);
                          } catch (error) {
                              console.error('Error deleting property:', error);
                              alert('Failed to delete property.');
                          }
                      }
                  };
              });
          }

          document.getElementById('addPropertyForm').addEventListener('submit', async function(event) {
              event.preventDefault();
              const propertyType = document.getElementById('addPropertyType').value;
              const name = document.getElementById('addPropertyName').value;

              try {
                  const response = await fetch(${API_BASE_URL}/admin/properties/${propertyType}, {
                      method: 'POST',
                      headers: getAuthHeaders(),
                      body: JSON.stringify({ name })
                  });
                  if (!response.ok) {
                      const errorData = await response.json();
                      throw new Error(errorData.detail || HTTP error! status: ${response.status});
                  }
                  alert('Property added successfully!');
                  bootstrap.Modal.getInstance(document.getElementById('addPropertyModal')).hide();
                  this.reset();
                  loadProperties(propertyType);
              } catch (error) {
                  console.error('Error adding property:', error);
                  alert(Failed to add property: ${error.message});
              }
          });

          document.getElementById('editPropertyForm').addEventListener('submit', async function(event) {
              event.preventDefault();
              const propertyType = document.getElementById('editPropertyType').value;
              const id = document.getElementById('editPropertyId').value;
              const name = document.getElementById('editPropertyName').value;

              try {
                  const response = await fetch(${API_BASE_URL}/admin/properties/${propertyType}/${id}, {
                      method: 'PUT',
                      headers: getAuthHeaders(),
                      body: JSON.stringify({ name })
                  });
                  if (!response.ok) {
                      const errorData = await response.json();
                      throw new Error(errorData.detail || HTTP error! status: ${response.status});
                  }
                  alert('Property updated successfully!');
                  bootstrap.Modal.getInstance(document.getElementById('editPropertyModal')).hide();
                  loadProperties(propertyType);
              } catch (error) {
                  console.error('Error updating property:', error);
                  alert(Failed to update property: ${error.message});
              }
          });

          // Event listener for property tabs to load data when activated
          document.getElementById('propertyTabs').addEventListener('shown.bs.tab', function (event) {
              const activeTabId = event.target.id; // e.g., 'difficulty-tab'
              let propertyType = '';
              if (activeTabId === 'difficulty-tab') {
                  propertyType = propertyTypeMap.difficulty;
              } else if (activeTabId === 'cognitive-tab') {
                  propertyType = propertyTypeMap.cognitive;
              } else if (activeTabId === 'learning-tab') {
                  propertyType = propertyTypeMap.learning;
              }
              if (propertyType) {
                  loadProperties(propertyType);
              }
          });

          // Event listener for Add Property buttons to set the property type in the modal
          document.querySelectorAll('[data-bs-target="#addPropertyModal"]').forEach(button => {
              button.addEventListener('click', function() {
                  const propertyType = this.dataset.propertyType;
                  document.getElementById('addPropertyType').value = propertyType;
                  document.getElementById('addPropertyModalLabel').textContent = Add New
  ${propertyType.replace('_', ' ').replace(/levels$/, '').trim()} Property;
              });
          });

          // Initial load
          document.addEventListener('DOMContentLoaded', () => {
              document.getElementById('logoutButton').addEventListener('click', function() {
                  localStorage.removeItem('accessToken');
                  localStorage.removeItem('isAdmin');
                  window.location.href = 'login.html';
              });
              loadUsers();
              // Load default active tab's properties
              loadProperties(propertyTypeMap.difficulty);
          });
      </script>
  </body>
  </html>