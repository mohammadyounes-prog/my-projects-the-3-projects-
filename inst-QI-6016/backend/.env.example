# Backend environment configuration

# JWT secret for signing tokens (required in prod)
SECRET_KEY=change_this_to_a_strong_random_secret

# External integrations (optional unless features used)
GOOGLE_API_KEY=
CSE_API_KEY=
GOOGLE_CSE_ID=

